
function recordUsage(label){try{const k='amase_usage_vpn_needs_router';const d=JSON.parse(localStorage.getItem(k)||'{"total":0,"labels":{}}');d.total++;d.labels[label]=(d.labels[label]||0)+1;localStorage.setItem(k,JSON.stringify(d));}catch(e){}}
const RULES={wifi:{title:"公衆Wi‑Fi中心",needs:["通信の暗号化","自動接続/キルスイッチ","スマホ対応","接続先の分かりやすさ"]},remote:{title:"リモート接続中心",needs:["固定IPまたは専用IPの要否確認","アクセス制御","複数端末管理","ログ/管理機能"]},fixed:{title:"固定IP中心",needs:["専用/共有固定IPの違い","接続元IP制限との互換性","利用地域","同時接続数"]},travel:{title:"旅行・出張中心",needs:["渡航先での利用可否","スマホ対応","速度と接続先","公衆Wi‑Fi保護"]}};
document.getElementById('go').onclick=()=>{const use=document.getElementById('use').value,dev=document.getElementById('devices').value,pri=document.getElementById('priority').value,r=RULES[use];let extra=[];if(dev==='many')extra.push('同時接続台数・端末管理');if(pri==='privacy')extra.push('ログ方針・運営主体の確認');if(pri==='speed')extra.push('近い接続拠点・速度試験');if(pri==='easy')extra.push('アプリの分かりやすさ・自動接続');if(pri==='admin')extra.push('チーム管理・権限分離');const needs=[...new Set([...r.needs,...extra])];const offers=(window.AFFILIATE_OFFERS||[]);const el=document.getElementById('result');el.hidden=false;el.innerHTML=`<h2>${r.title}</h2><p>まず見る項目はこれ。</p><div class="chips">${needs.map(x=>`<span class="chip">${x}</span>`).join('')}</div><p class="muted">VPNは用途で必要条件がかなり変わります。契約前に公式の対応OS、同時接続数、料金、返金条件を確認してください。</p>${offers.length?`<div class="ad"><strong>候補サービス（広告を含みます）</strong>${offers.map(o=>`<p><a rel="sponsored noopener" target="_blank" href="${o.url}">${o.name}</a> <small>${o.disclosure||'広告'}</small></p>`).join('')}</div>`:''}`;};