# Changelog

## Unreleased
- Filter enabled affiliate URLs to `http` / `https` before rendering.
- Escape affiliate names, disclosures and href values before inserting them into HTML.
- Add regression coverage for malicious schemes and markup-like configuration values.

## 1.0.0
- Initial public release
- Four use-case routes
- Affiliate slots disabled by default
